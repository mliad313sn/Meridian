strict-ssl=false
# Local workaround: a Cisco Umbrella TLS-inspection proxy re-signs registry
# traffic and its root CA is not in the Windows trust store, so npm burns ~70s
# of retries per fetch. Remove this line once the Umbrella root CA is installed.
